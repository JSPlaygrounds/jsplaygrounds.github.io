/* eslint-disable import/prefer-default-export */
import PropTypes from 'prop-types';

export const playgroundShape = PropTypes.shape({
  name: PropTypes.string,
  path: PropTypes.string,
  description: PropTypes.string,
});
